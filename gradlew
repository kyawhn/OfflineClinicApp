#!/usr/bin/env sh

##############################################################################
##
##  Gradle start up script for UN*X
##
##############################################################################

# Set APP_HOME if not already set
APP_HOME=$(dirname "$0")

# Run gradle wrapper
exec "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" "$@"